frontend/node_modules/
backend/node_modules/
dist/
uploads/

node_modules/

# Secrets & local config
.env
.env.local
.env.*.local

# QQ state autosave (Recovery nach Crash/Sleep)
.qq-rooms/
backend/.qq-rooms/

# Roh-Asset-Zips (z.B. Wolfs Icon-/Logo-Lieferungen) — nie ins public/-Deploy
frontend/public/**/*.zip
# Lokale Roh-Quell-Assets (Zip-Lieferungen etc.) — nicht committen, nicht bauen
_source-assets/
# Wolfs Arena-Background-Rohlieferung (68MB PNG) — die komprimierten WebP liegen
# in frontend/public/arena-bg/, die Originale bleiben nur lokal.
frontend/public/neue background/
